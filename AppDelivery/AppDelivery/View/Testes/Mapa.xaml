<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:map="clr-namespace:Xamarin.Forms.GoogleMaps;assembly=Xamarin.Forms.GoogleMaps"
             mc:Ignorable="d"
             x:Class="AppDelivery.View.Mae">

    <!--<Grid>
        <map:Map x:Name="meuMapa" 
                 MapType="Street" 
                 HasZoomEnabled="True" 
                 MyLocationEnabled="True">
            <map:Map.ItemTemplate>
                <DataTemplate>
                    <map:Pin Icon="{Binding pinIcon}"
                             Position="{Binding pinPosition}"
                             Label="{Binding pinLabel}"/>
                </DataTemplate>
            </map:Map.ItemTemplate>
        </map:Map>
    </Grid>-->

  

        <Grid>
            <map:Map x:Name="meuMapa"
                     MapType="Street"
                     HasZoomEnabled="True"
                     MyLocationEnabled="True">
            </map:Map>
        </Grid>
        

    
</ContentPage>